Environment variables

ex:
     $LANG  // this used to set the language

Accessing the variables
    echo $variablename 

$PATH 
  this will tells that what are the directories in which system looks for executable files.

if we enter any command in terminal then it will search the programm in directories
mentioned in path variable.

$USER -- tells user name
$HOME -- tells the home dir name

env -- tells all the env variables


set environment variables
----------------------------
NEWVARIABLE = value // this will create new vairable

delete variable
----------------------
unset NEWVARIABLE