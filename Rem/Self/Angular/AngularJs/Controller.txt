
Two things that happen when we create a new controller using the entry controller directive.
1. create a new scope around this element.
2. create an instance of the controller, & inject it with the new scope we just created
